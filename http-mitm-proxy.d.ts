/**
 * Type declaration for vendored http-mitm-proxy (source in ./http-mitm-proxy).
 * The vendored code is excluded from type-checking; this provides minimal types for the project.
 */
declare module 'http-mitm-proxy' {
  export const Proxy: {
    new (): unknown;
    gunzip: unknown;
    wildcard: unknown;
  };
}
